@model UcherMBlog.Models.Article


@{
    ViewBag.Title = @Model.Title;
}

<div class="row">
    <div class="col-md-10 col-md-offset-1">
        <h2>@Model.Title</h2>
        @Html.Raw(@Model.Content.Content)
    </div>
</div>
